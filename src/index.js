import React from 'react';
import ReactDOM from 'react-dom/client';

const appContent = <h1>Welcome to my App</h1>

const root = ReactDOM.createRoot(document.getElementById('app'));
root.render(appContent);
